% !TeX spellcheck = en_US
% !TeX encoding = UTF-8

% COMPILE WITH:
% `latexmk`
% You need lualatex and biber (in all TeXLive distributions)

\documentclass[
    numbers=noenddot,
    %listof=totoc,
    parskip=half-,
    fontsize=12pt,
    paper=a4,
    oneside,
    titlepage,
    bibliography=totoc,
    chapterprefix=false,
%    draft
]{scrbook}

% use lualatex or xelatex
%\usepackage{fontspec}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{float}
\usepackage[onehalfspacing]{setspace}

% better language support
%\usepackage{polyglossia}
%\setdefaultlanguage{english}
%\setotherlanguage{german}

\usepackage{tocbasic}
\usepackage{booktabs}
\usepackage{multicol}
\usepackage{multirow}
\usepackage{longtable}

\usepackage[]{scrlayer-scrpage}

% better bibliography (biblatex style)
% use biber to compile
% Ensure encoding/font packages are loaded before biblatex and csquotes
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{times}
\usepackage{latexsym}
\usepackage{inconsolata}

\usepackage[citestyle=alphabetic, bibstyle=alphabetic, sorting=nyt, backend=biber, language=english, backref=true, maxcitenames=2]{biblatex}

% better quotes
% use \enquote{text}
\usepackage[autostyle,english=american,german=quotes]{csquotes}
\addbibresource{references.bib}
\addbibresource{anthology.bib}

% appendix
\usepackage[titletoc]{appendix}

% where to put all images and figures
\graphicspath{{images/}}

% allow including SVG images via \includesvg
\usepackage{svg}

% YOUR ADDITIONAL PACKAGES

% Make table of contents entries and references clickable
% Load hyperref late in the preamble with hidelinks to avoid colored boxes
\usepackage[hidelinks]{hyperref}
\hypersetup{
  pdftitle={Zero-Shot Narrative Classification via Agentic and Actor-Critic LLM Pipelines},
  pdfauthor={Nour Eljadiri},
  pdfencoding=auto,
  bookmarksopen=true
}

% framed note box (lightweight, uses tcolorbox)
\usepackage[most]{tcolorbox}
\newtcolorbox{notebox}{colback=gray!8,colframe=black!50,boxrule=0.5pt,arc=2pt,left=1mm,right=1mm,top=1mm,bottom=1mm}

% Title
\title{Leveraging LLMs for Hierarchical Multi-Label Classification of Narratives}

% Author
\author{Nour Eljadiri}

% Date
\date{\today}

% CHOOSE ACCORDINGLY
%\newcommand{\thesisType}{Bachelorarbeit}
\newcommand{\thesisType}{Masterarbeit}

\makeatletter
\let\thetitle\@title
\let\theauthor\@author
\let\thedate\@date
\makeatother

\pagestyle{scrheadings}

\begin{document}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\frontmatter
% CHOOSE ACCORDINGLY
%\include{includes/BA-titlepage}
\include{includes/MA-titlepage}

\tableofcontents
\newpage

% -- ABSTRACT
\include{includes/abstract}
\newpage

% -- Acknowledgements (optional)
\include{includes/acknowledgements}
\newpage

% % -- List of figures
% \thispagestyle{empty}
% \cleardoublepage
% \listoffigures
% \newpage
% 
% % -- List of tables
% \thispagestyle{empty}
% \cleardoublepage
% \listoftables
% \newpage

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\mainmatter

% -- Chapters
\include{chapters/introduction}
\include{chapters/related_work}
\include{chapters/task_and_data_analysis}
\include{chapters/system_architectures}
\include{chapters/experimental_setups}
\include{chapters/results}
\include{chapters/error_analysis}
\include{chapters/discussion}
\include{chapters/conclusion}

% -- Appendix
\begin{appendices}
    \input{chapters/appendix.tex}
\end{appendices}
\newpage


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\backmatter

% -- Bibliography
\printbibliography

% -- Eidesstattliche Erklärung (= Affidavit)
\include{includes/eidesstattlicheErklaerung}


\end{document}
